# Vite Environment Variables
# Variables prefixed with VITE_ are exposed to the client-side code
# Variables without VITE_ prefix are only available during build time

# Solana RPC endpoints (exposed to client)
VITE_SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
VITE_SOLANA_RPC_URL_DEVNET=https://api.devnet.solana.com
VITE_SOLANA_RPC_URL_TESTNET=https://api.testnet.solana.com
VITE_BACKEND_URL=http://localhost:4000
# App configuration (exposed to client)
VITE_APP_NAME=Solana Wallet Client
VITE_APP_VERSION=1.0.0
VITE_NETWORK=devnet

# API Keys (exposed to client - be careful with sensitive data)
VITE_COINGECKO_API_KEY=your_coingecko_api_key_here

# Build-time only variables (NOT exposed to client)
DATABASE_URL=your_database_url_here
SECRET_KEY=your_secret_key_here